import NavLogo from "components/NavLogo/NavLogo";

const Header = () => {
  return (
    <header className="border-b-2 border-accent mb-3">
      <NavLogo />
    </header>
  );
};

export default Header;
